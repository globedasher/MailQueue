###This is the MailQueue

Modules of interest:
argparser for getting args from the CLI.
zeromq for inter-process communcation.

Run `python mail_queue.py -h` to get all the arguments available with help
text.
